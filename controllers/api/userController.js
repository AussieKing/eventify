// Manages API requests related to user authentication, registration, and profile management.
const router = require("express").Router();

// When my account button is pressed, direct user to my account page
// Then load all the users favourited & joined events on that page.


module.exports = router;
